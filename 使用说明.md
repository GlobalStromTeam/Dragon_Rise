# DragonRise:Reforge 载具数据填写使用说明

本文档说明如何使用本项目 datagen 生成器体系 + AI 提示词系统来添加/完善载具数据。

## 一、概览

`gradlew runData` 会执行一套生成器，自动完成：

| 生成器 | 作用 |
|---|---|
| GeoOBBDataProvider | 从模型提取 OBB/炮塔/座位/射击位/地形兼容，写车辆 JSON |
| VehicleJavaGenerator | 为有 Build 骨骼的模型生成实体类/渲染器 + 注册 |
| VehicleSkinGenerator | 按 `{载具ID}_{涂装ID}.png` 命名自动生成皮肤数据 |
| VehicleDataCompletenessChecker | 生成缺失报告 + AI 填充提示词 |
| MeshModelNormalRemover | 删除模型中的 normals 字段 |
| VehicleLanguageGenerator | 同步语言文件 |

## 二、模型规范（新载具建模要求）

模型放在 `src/main/resources/assets/dragonrise_reforge/models/bedrock/vehicle/{载具ID}.geo.json`。

### 1. Build 骨骼（必需）
模型中必须有名为 `Build` 的骨骼——这是"该载具走自动生成流程"的标记。**没有 Build 骨骼的载具会被生成器跳过**（保护已手工做好的老车）。

### 2. 类型标记骨骼（推荐）
加一个空骨骼，命名为 `TANK` / `CAR` / `PLANE` / `HELI` / `SHIP` / `AIRSHIP`（大小写不限），决定该载具的类型，AI 提示词会按类型给出对应字段指南：
- `TANK` 坦克（300-500 血）　`CAR` 轮式（100-300 血）
- `PLANE` 固定翼 / `HELI` 直升机 / `AIRSHIP` 飞艇（100-300 血）
- `SHIP` 舰船

没有类型骨骼时默认按 `CAR` 处理。

### 3. 功能骨骼（可选，自动识别）
- 射击点：`CannonPos*` → `Cannon`、`MachineGunPos*` → `MachineGun`、`MissilePos*` → `Missile`
- 座位：`SeatsPos*`、座位相机：`SeatsCameraPos*`
- 地形兼容：`TerrainCompatPos*`
- 碰撞盒：`*Obb*`（`CollisionObb` → 纯碰撞 `Part: Collision`；`TurretObb` → 炮塔；`WheelLeftObb`/`WheelRightObb`/`MainEngineObb`）

## 三、跑 datagen

```
gradlew runData
```

输出：
- `src/main/.../sbv/vehicles/*.json`（骨架数据）
- `src/main/java/.../entities/`、`client/renderer/`（Java 类 + 注册）
- `datagen_vehicle_report.md`（完整性报告）
- `datagen_ai_prompts/{id}.md`（AI 提示词）
- `data/.../vehicle_skins/*.json`（皮肤数据）

## 四、AI 提示词工作流

1. 模型（含 Build 骨骼）放好 → `gradlew runData`
2. 看 `datagen_vehicle_report.md`——哪些载具缺核心字段（`EngineInfo`/`Weapons`）
3. 把 `datagen_ai_prompts/{载具ID}.md` 给 AI（或本项目的 AI 助手）
4. AI 按提示词填出完整 JSON → 写回 `src/main/resources/data/dragonrise_reforge/sbw/vehicles/{id}.json`
5. 再跑 `gradlew runData` 验证——该车从"缺少"移到"核心齐全"

## 五、提示词包含的参考

AI 提示词内嵌以下官方实测参考：

- **类型专属指南**：HudType / EngineType / EngineInfo 键 / 特有字段 / 无需字段
- **官方参考载具**：坦克→M1A2、轮式→LAV-25、固定翼→A-10、直升机→米-28、舰船→快艇、飞艇→基洛夫
- **武器完整配置示例**：坦克炮 / 机炮 / 机枪 / 导弹 / 火箭
- **Projectile 抛射物**、**AmmoType 弹药**参考
- **Velocity 初速**：直线 20-35、曲射 <15、航弹 0.8-1.5、导弹 2-6
- **ProjectileLife 存活**：直线 20-60、曲射 800（官方实测）
- **Spread 散布**：主炮 0.02-0.06、机炮 0.25-3、航弹 10
- **速度换算**：Increment/Decrement 加速度建议 0.10；MaxForwardSpeedRate 填 1 = 80 km/h、MaxBackwardSpeedRate 填 1 = 64 km/h
- **Type / EngineType / HudType / ContainerIcon** 可选值清单

## 六、添加皮肤（喷漆罐涂装）

1. 贴图命名为 `{载具ID}_{涂装ID}.png`（如 `brdm2_sand.png`）放进 `textures/entity/`
2. `gradlew runData` → 自动生成 `vehicle_skins` 皮肤数据（含名称映射，可改 `VehicleSkinGenerator` 的 `SKIN_NAMES`）
3. 默认贴图：改车辆 JSON 的 `Models[].Texture` 指向基础涂装

## 七、注意事项

- `Weapons` 非必填：纯运输车/无射击点骨骼的载具**省略 Weapons**，不要硬填
- `geo/` 目录是 GeckoLib 专用（防具/抛射物模型），**SBM 车辆模型不要放那里**（GeckoLib 启动会扫描 `geo/`，SBM 模型会崩溃）
- 皮肤数据放 `data/.../vehicle_skins/`（服务器加载器读取；assets 那份是冗余副本）
- 已有载具（模型无 Build 骨骼）不会被生成器覆盖，可放心迭代
